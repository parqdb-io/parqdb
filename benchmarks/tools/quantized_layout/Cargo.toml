[package]
name = "relify-quantized-layout-benchmark"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
arrow = "=58.3.0"
parquet = { version = "=58.3.0", features = ["arrow"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"

[profile.release]
codegen-units = 1
lto = "thin"
